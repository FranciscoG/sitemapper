{
  "name": "sitemapper",
  "version": "0.1.0",
  "description": "Site crawler that builds an XML sitemap",
  "main": "sitemapper.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=0.10.0"
  },
  "dependencies": {
    "simplecrawler": "^0.5.4",
    "yargs": "^3.27.0"
  }
}
